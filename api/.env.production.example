# ============================================
# UndangKita API - Production Environment
# ============================================
# Copy this file to .env and update the values
# IMPORTANT: Never commit your actual .env file to version control!

# Application Settings
NODE_ENV=production
PORT=8000
BASE_URL=https://your-production-domain.com
ALLOWED_ORIGINS=http://your-frontend-domain.com

# Database Configuration
# PostgreSQL connection string (REQUIRED - update with your production database credentials)
DATABASE_URL=postgresql://your_db_user:your_strong_password@your_db_host:5432/semeru_db

# PostgreSQL credentials (for docker-compose)
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_strong_password
POSTGRES_DB=semeru_db

# JWT Configuration
# IMPORTANT: Generate a strong, random secret key for production!
# You can use: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this-immediately
# Token expiration time (e.g., 1h, 24h, 7d)
JWT_EXPIRES_IN=24h

# Logging Configuration
# Available levels: DEBUG, INFO, WARN, ERROR
# Recommended: INFO or WARN for production
# In production, logs are written to database instead of files
LOG_LEVEL=INFO
# Directory where log files will be stored (only used in development)
LOG_DIRECTORY=logs

# Supabase Configuration (REQUIRED for production file uploads)
# Get these values from your Supabase project settings
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_BUCKET_NAME=undangan-photos

# ============================================
# Production Security Checklist:
# ============================================
# [ ] Generate a strong JWT_SECRET using: openssl rand -base64 64
# [ ] Use a strong database password
# [ ] Ensure DATABASE_URL points to your production database
# [ ] Set LOG_LEVEL to INFO or WARN (not DEBUG)
# [ ] Configure Supabase storage bucket with proper RLS policies
# [ ] Configure proper firewall rules
# [ ] Set up SSL/TLS for database connections
# [ ] Use a reverse proxy (nginx/traefik) with HTTPS
# [ ] Enable rate limiting on your reverse proxy
# [ ] Set up database backups
# ============================================

# Super Admin Seeder Password
SEEDER_PASSWORD=semeru12345